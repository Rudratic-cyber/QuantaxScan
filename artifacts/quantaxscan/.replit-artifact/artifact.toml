kind = "web"
previewPath = "/"
title = "QuantaXscan — Post-Quantum Security Scanner"
version = "1.0.0"
id = "artifacts/quantaxscan"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20714

[services.development]
run = "pnpm --filter @workspace/quantaxscan run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/quantaxscan", "run", "build" ]
publicDir = "artifacts/quantaxscan/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20714"
BASE_PATH = "/"
