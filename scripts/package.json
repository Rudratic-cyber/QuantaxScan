{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "cleanup-orphans": "tsx ./src/cleanup-orphans.ts",
    "backfill-assets": "tsx ./src/backfill-assets.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check-standards-freshness": "tsx ./src/check-standards-freshness.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@workspace/collectors": "workspace:*",
    "@workspace/db": "workspace:*",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "vitest": "^4.1.10"
  }
}
